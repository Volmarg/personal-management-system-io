export default class SearchModule {
    static readonly SUPPORTED_MODULE_NOTES      = "notes";
    static readonly SUPPORTED_MODULES_PASSWORDS = "passwords";

    static readonly ALL_SUPPORTED_MODULES = [
      SearchModule.SUPPORTED_MODULE_NOTES,
      SearchModule.SUPPORTED_MODULES_PASSWORDS,
    ];
}